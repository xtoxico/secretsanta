<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 2rem;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.6;
            color: #333;
            background: #fdf2e9;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-top: 2rem;
        }

        h1 {
            color: #d35400;
            text-align: center;
        }

        label {
            font-weight: bold;
            color: #555;
        }

        input,
        textarea {
            border: 1px solid #ddd;
            border-radius: 6px;
            font-family: inherit;
        }

        button {
            transition: background 0.3s;
        }

        button:hover {
            background: #2471a3 !important;
        }
    </style>
</head>

<body>
    <h1>Unirse a: <%= roomName %>
    </h1>

    <div class="card">
        <form action="/unirse/<%= roomId %>" method="POST">
            <div style="margin-bottom: 1.5rem;">
                <label for="name" style="display: block; margin-bottom: 0.5rem;">Tu Nombre:</label>
                <input type="text" id="name" name="name" required
                    style="width: 100%; padding: 0.8rem; box-sizing: border-box;">
            </div>

            <div style="margin-bottom: 1.5rem;">
                <label for="giftHint" style="display: block; margin-bottom: 0.5rem;">Pista para tu regalo (¿Qué te
                    gusta?):</label>
                <textarea id="giftHint" name="giftHint" required
                    style="width: 100%; padding: 0.8rem; box-sizing: border-box;" rows="3"></textarea>
            </div>

            <button type="submit"
                style="background: #3498db; color: white; padding: 1rem; border: none; border-radius: 6px; cursor: pointer; font-size: 1.1rem; width: 100%; font-weight: bold;">Unirme
                al Grupo</button>
        </form>
    </div>
</body>

</html>