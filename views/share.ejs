<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 2rem;
            max-width: 800px;
            margin: 0 auto;
            line-height: 1.6;
            color: #333;
            background: #fdf2e9;
            text-align: center;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-top: 2rem;
        }

        .link-box {
            background: #f8f9fa;
            border: 2px dashed #3498db;
            padding: 1.5rem;
            margin: 1.5rem 0;
            text-align: left;
        }

        .link-item {
            margin-bottom: 1rem;
        }

        .link-label {
            font-weight: bold;
            color: #d35400;
            display: block;
            margin-bottom: 0.3rem;
        }

        .link-url {
            font-family: monospace;
            font-size: 1.1rem;
            word-break: break-all;
            color: #2980b9;
        }

        .btn {
            display: inline-block;
            background: #2ecc71;
            color: white;
            padding: 0.8rem 1.5rem;
            text-decoration: none;
            border-radius: 6px;
            margin-top: 1rem;
            font-weight: bold;
        }

        h1 {
            color: #d35400;
        }

        h2 {
            color: #2c3e50;
        }
    </style>
</head>

<body>
    <h1>¡Grupo Creado! 🎅</h1>

    <div class="card">
        <h2>
            <%= roomName %>
        </h2>
        <p>Tu grupo de Amigo Invisible está listo. Comparte los siguientes enlaces:</p>

        <div class="link-box">
            <div class="link-item">
                <span class="link-label">1. Para Participantes (Inscribirse):</span>
                <div class="link-url">
                    <%= links.join %>
                </div>
            </div>

            <div class="link-item">
                <span class="link-label">2. Lista de Inscritos (Ver quién se ha unido):</span>
                <div class="link-url">
                    <%= links.list %>
                </div>
            </div>

            <div class="link-item">
                <span class="link-label">3. Sorteo y Resultados (Link del Administrador/Resultado):</span>
                <div class="link-url">
                    <%= links.draw %>
                </div>
            </div>
        </div>

        <p><strong>IMPORTANTE:</strong> Guarda bien estos enlaces. No hay base de datos, ¡así que no los pierdas!</p>

        <a href="/" class="btn">Volver al Inicio</a>
    </div>
</body>

</html>