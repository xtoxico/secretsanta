<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Amigo Invisible</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 2rem;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.6;
            color: #333;
            background: #fdf2e9;
        }

        h1 {
            color: #d35400;
            text-align: center;
        }

        .card {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-top: 1rem;
        }

        label {
            font-weight: bold;
            color: #555;
        }

        input {
            border: 1px solid #ddd;
            border-radius: 6px;
        }

        button {
            transition: background 0.3s;
        }

        button:hover {
            background: #c0392b !important;
        }
    </style>
</head>

<body>
    <h1>🎅 Amigo Invisible 🎄</h1>
    <div class="card">
        <h2 style="text-align: center; margin-top: 0;">Crear Nuevo Grupo</h2>
        <form action="/crear-sala" method="POST">
            <div style="margin-bottom: 1.5rem;">
                <label for="nombreGrupo" style="display: block; margin-bottom: 0.5rem;">Nombre del Grupo:</label>
                <input type="text" id="nombreGrupo" name="nombreGrupo" required
                    style="width: 100%; padding: 0.8rem; box-sizing: border-box;">
            </div>

            <div style="margin-bottom: 1.5rem;">
                <label for="fechaMaxima" style="display: block; margin-bottom: 0.5rem;">Cuándo se dan los
                    regalos:</label>
                <input type="date" id="fechaMaxima" name="fechaMaxima" required
                    style="width: 100%; padding: 0.8rem; box-sizing: border-box;">
            </div>

            <div style="margin-bottom: 1.5rem;">
                <label for="precioMaximo" style="display: block; margin-bottom: 0.5rem;">Precio Máximo
                    (Opcional):</label>
                <input type="text" id="precioMaximo" name="precioMaximo" placeholder="ej: 20€"
                    style="width: 100%; padding: 0.8rem; box-sizing: border-box;">
            </div>

            <button type="submit"
                style="background: #e74c3c; color: white; padding: 1rem; border: none; border-radius: 6px; cursor: pointer; font-size: 1.1rem; width: 100%; font-weight: bold;">Crear
                Sala</button>
        </form>
    </div>
</body>

</html>